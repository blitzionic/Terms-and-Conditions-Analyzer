'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _harRequest = require('./har-request');

var _harRequest2 = _interopRequireDefault(_harRequest);

var _harResponse = require('./har-response');

var _harResponse2 = _interopRequireDefault(_harResponse);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var HAREntry = function () {
  function HAREntry(entry) {
    _classCallCheck(this, HAREntry);

    this.request = new _harRequest2.default(entry.request);
    this.response = new _harResponse2.default(entry.response);
  }

  _createClass(HAREntry, [{
    key: 'isImage',
    value: function isImage() {
      return this.response.isImage();
    }
  }, {
    key: 'getImageName',
    value: function getImageName() {
      var name = null;

      if (this.isImage()) {
        var url = this.request.url;
        var pos = url.pathname.lastIndexOf('/');
        name = url.pathname.substring(pos + 1);
      }

      return name;
    }
  }]);

  return HAREntry;
}();

exports.default = HAREntry;